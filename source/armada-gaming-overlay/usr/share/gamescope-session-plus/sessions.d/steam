#!/usr/bin/env bash
set -euo pipefail

if [ -f /usr/libexec/armada/device-env ]; then
  eval "$(/usr/libexec/armada/device-env)"
fi

export STEAM_GAMESCOPE_VRR_SUPPORTED=1
export STEAM_GAMESCOPE_HAS_TEARING_SUPPORT=1
export STEAM_GAMESCOPE_TEARING_SUPPORTED=1
export STEAM_GAMESCOPE_HDR_SUPPORTED=0
export STEAM_GAMESCOPE_DYNAMIC_FPSLIMITER=1
export STEAM_GAMESCOPE_NIS_SUPPORTED=1
export STEAM_GAMESCOPE_FANCY_SCALING_SUPPORT=1
export STEAM_GAMESCOPE_COLOR_MANAGED=1
export STEAM_GAMESCOPE_VIRTUAL_WHITE=1
export STEAM_USE_DYNAMIC_VRS=1

export STEAM_MANGOAPP_PRESETS_SUPPORTED=1
export STEAM_USE_MANGOAPP=1
export STEAM_DISABLE_MANGOAPP_ATOM_WORKAROUND=1
export STEAM_MANGOAPP_HORIZONTAL_SUPPORTED=1

export STEAM_ENABLE_VOLUME_HANDLER=1
export STEAM_MULTIPLE_XWAYLANDS=1
export STEAM_ALLOW_DRIVE_UNMOUNT=1
export STEAM_ALLOW_DRIVE_ADOPT=1
export STEAM_DISABLE_AUDIO_DEVICE_SWITCHING=1

export SRT_URLOPEN_PREFER_STEAM=1

export STEAMOS_STEAM_REBOOT_SENTINEL="/tmp/steamos-reboot-sentinel"
export REBOOT_SENTINEL="/tmp/steamos-reboot-sentinel"
export STEAMOS_STEAM_SHUTDOWN_SENTINEL="/tmp/steamos-shutdown-sentinel"
export SHUTDOWN_SENTINEL="/tmp/steamos-shutdown-sentinel"

export QT_IM_MODULE=steam
export GTK_IM_MODULE=Steam

export CURSOR_FILE="${HOME}/.local/share/Steam/tenfoot/resource/images/cursors/arrow.png"
export XCURSOR_THEME=steam
export XCURSOR_SCALE=256

export GAMESCOPE_FALLBACK_APPID=0x41524d41

CLIENTCMD="/usr/libexec/armada/launch-steam -gamepadui -steamos3 -steampal -steamdeck"

if [ -d "/opt/gamescope" ]; then
  export PATH="/opt/gamescope/bin:${PATH}"
fi

if [ -x /usr/bin/steam_notif_daemon ]; then
  /usr/bin/steam_notif_daemon &
fi

exec gamescope -e -- /usr/libexec/armada/launch-steam -gamepadui -steamos3 -steampal -steamdeck
