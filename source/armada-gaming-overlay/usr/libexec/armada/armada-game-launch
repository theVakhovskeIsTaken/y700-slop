#!/usr/bin/env bash
set -euo pipefail

GAMECMD="$*"

if [ -f /usr/libexec/armada/device-env ]; then
  eval "$(/usr/libexec/armada/device-env)"
fi

export DXVK_HUD="${DXVK_HUD:-none}"

if [ -n "${ARMADA_BIG_CORES:-}" ]; then
  export WINE_CPU_TOPOLOGY="${ARMADA_BIG_CORES}"
fi

exec $GAMECMD
